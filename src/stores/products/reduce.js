import * as types from './action-type'

const initState = []

// export const proData = (state = initState, action) => {
//   switch (action.type) {
//     case types.GET_PRODUCT:
//       return [...state, ]
//   }
// }